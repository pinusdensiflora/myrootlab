<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rootlab.practcomm.reservation.mapper.ImgDTOMapper">
	<insert id="r_save" 
		parameterType="com.rootlab.practcomm.reservation.dto.ImgDTO">
		INSERT INTO r_img 
		(collection, datetime, display_sitename, doc_url, 
		height, image_url, thumbnail_url, width, reservation_id)
		VALUES
		(#{collection}, #{datetime}, #{display_sitename}, #{doc_url}, 
		#{height}, #{image_url}, #{thumbnail_url}, #{width}, reservation_id);
	</insert>


</mapper>