<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rootlab.practcomm.img.ImgMetaMapper">
	<insert id="save" 
		parameterType="com.rootlab.practcomm.img.ImgMeta">
		INSERT INTO imgmeta 
		(collection, datetime, display_sitename, doc_url, 
		height, image_url, thumbnail_url, width, memo)
		VALUES
		(#{collection}, #{datetime}, #{display_sitename}, #{doc_url}, 
		#{height}, #{image_url}, #{thumbnail_url}, #{width}, #{memo});
	</insert>

</mapper>

