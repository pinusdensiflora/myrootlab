<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rootlab.practcomm.reservation.mapper.VideoDTOMapper">

	<insert id="r_save" parameterType="com.rootlab.practcomm.reservation.dto.VideoDTO">
		INSERT INTO r_video (author, datetime, play_time, thumbnail, title, url, createtime, reservation_id )
		VALUES
		(#{author}, #{datetime}, #{play_time}, #{thumbnail}, #{title}, #{url}, #{createtime}, #{reservation_id});
	</insert>


</mapper>